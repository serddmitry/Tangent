New since v0.12.3-dima.12:

Upstream
+ Merged upstream Tangent **v0.12.10** into this fork. // The fork was based on v0.12.2; this pulls in everything through v0.12.10 and re-bases the fork's version onto the next patch (`0.12.11-dima.1`).
+ Added **Copy Relative Path** alongside the existing copy-path command, space-drag panning and shift-drag scroll-direction in PDFs and images, "Open to the Right/Left/Replace" on sidebar items, and commands for activating color 🟪highlights🟪 (bindable to shortcuts).
+ Made "safe header" names much more lenient, so [[wiki links]] can target non-latin headers and headers with adjacent spaces (e.g. `My - Header`). // Fixes several header-matching bugs from upstream.
- Pulled in upstream fixes for Focus Mode text dimming lingering after switching items, blank ordered-list lines on Enter, list-shifting garbling glyphs, wiki-link autocomplete, and mis-aligned PDF/image zoom sliders.

Checkboxes
// Upstream v0.12.9 also added its own checkbox-toggle command on `Cmd+[`. This fork keeps its existing checkbox commands instead: **Toggle Checkbox** (`Mod+Enter`) and **Toggle Checkbox Canceled** (`Alt+Enter`), which support the canceled state. // Upstream's `defaultTodoCompleteChar` setting still applies.
