New since v0.12.3-dima.4:

Saving & Data Safety
- Fixed a note opening completely empty (only its backlinks showing) even though the file on disk was intact. Unloading a note from memory was spuriously flagging it as having unsaved edits, and the dirty-file guard added last version then refused to load the real contents back from disk on reopen. // Your text was never at risk — the empty state lived only in memory, and empty content is never written to disk. Restarting cleared it; now it can't happen in the first place.
