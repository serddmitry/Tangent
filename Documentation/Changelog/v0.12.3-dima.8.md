New since v0.12.3-dima.7:

Wiki Links
- The `[[` autocomplete popup now lists notes with the most-recently-opened first, instead of by modified date. Notes you haven't opened this session still fall back to modified-date order below the ones you have. // Recency is tracked per navigation and seeded from the session history restored on launch, so the notes you were just in stay near the top.

Quick Open
- The Cmd+O file list uses the same most-recently-opened ordering. // Both popups share the same sort, so a note you just visited is one keystroke away in either.
