New since v0.12.3-dima.2:

Appearance
+ Inline `code` spans now render in their own color rather than borrowing the accent color that links use, so code and links no longer read alike.
+ Added an "Underline Links" setting (Settings → Appearance). When enabled, links in notes stay underlined instead of only underlining on hover.
