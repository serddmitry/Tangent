New since v0.12.3-dima.5:

Wiki Links
- Fixed the `[[` autocomplete popup ordering notes edited on the same day alphabetically instead of by recency. A note you just created or touched now sorts to the top, above notes you edited earlier today. // Previously the sort only compared dates down to the calendar day, so same-day notes fell back to A→Z name order.
