New since v0.12.3-dima.1:

Windows & Workspaces
- Fixed Tangent asking you to pick a workspace after its last window was closed. On macOS the app keeps running without windows; clicking the dock icon now reopens the workspace you were in instead of the selection screen.
- Fixed quitting with no windows open forgetting what was open, which made the *next* launch ask for a workspace as well. // The set of open workspaces is now remembered from the moment the last window closed.
