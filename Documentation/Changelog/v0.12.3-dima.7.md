New since v0.12.3-dima.6:

Search
- Fixed the caret jumping backwards when you edit a note with the in-document search open. Typing (or pressing Backspace) just after a word whose start matches the search term snapped the caret onto that earlier match instead of staying where you were typing. // Editing re-ran the search and rebuilt the match highlights, and that rebuild was re-selecting the current match as a side effect.
